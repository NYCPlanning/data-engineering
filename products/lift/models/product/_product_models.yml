version: 2

models:
- name: lift_supplemented
  description: >
    Supplemented version of lift_csv with the displacement_risk_formula,
    cpspenttotal, and cpprojects placeholder columns populated.
  columns:
  - name: bbl
    tests:
    - not_null
    - unique
