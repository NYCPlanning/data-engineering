name: cpdb

profile: dcp-de-postgres

model-paths: [ "models" ]

tests:
  +store_failures: true
  schema: "_tests"

models:
  cpdb:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    product:
      +materialized: table

flags:
  fail-fast: true
