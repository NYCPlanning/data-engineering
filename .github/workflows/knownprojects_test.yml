name: KPDB - Test

on:
  workflow_call:

jobs:
  branch_test_build:
    name: Build test run
    uses: ./.github/workflows/knownprojects_build.yml
    with:
      run_export: true
    secrets: inherit
