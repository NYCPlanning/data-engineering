version: 2

models:
- name: int__lion
  columns:
  - name: segmentid
    tests: [ not_null ]
  - name: globalid
    tests:
    - not_null
    # this is warn while source data issues with sectionalmap and zip code are resolved
    - unique:
        config:
          severity: warn
  - name: lionkey
    tests:
    - not_null
    # this is warn while source data issues with sectionalmap and zip code are resolved
    # additionally, there are some lionkey collisions in 25d: 1004700010
    - unique:
        config:
          severity: warn
