source_up

# Source environment variables from .env file if it exists
if [ -f .env ]; then
	set -a
	source .env
	set +a
fi

export PRODUCT_METADATA_REPO_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")/../product-metadata" && pwd)"
export DATA_ENGINEERING_REPO_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
