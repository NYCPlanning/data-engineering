SELECT
    a.ccpversion,
    a.maprojid,
    a.magencyacro,
    a.magency,
    a.magencyname,
    a.description,
    a.projectid,
    c.mindate,
    c.maxdate,
    d.typecategory,
    a.plannedcommit_ccnonexempt,
    a.plannedcommit_ccexempt,
    a.plannedcommit_citycost,
    a.plannedcommit_nccstate,
    a.plannedcommit_nccfederal,
    a.plannedcommit_nccother,
    a.plannedcommit_noncitycost,
    a.plannedcommit_total,
    c.spent_total_checkbooknyc,
    e.adopt_ccnonexempt,
    e.adopt_ccexempt,
    e.adopt_citycost,
    e.adopt_nccstate,
    e.adopt_nccfederal,
    e.adopt_nccother,
    e.adopt_noncitycost,
    e.adopt_total,
    e.allocate_ccnonexempt,
    e.allocate_ccexempt,
    e.allocate_citycost,
    e.allocate_nccstate,
    e.allocate_nccfederal,
    e.allocate_nccother,
    e.allocate_noncitycost,
    e.allocate_total,
    e.commit_ccnonexempt,
    e.commit_ccexempt,
    e.commit_citycost,
    e.commit_nccstate,
    e.commit_nccfederal,
    e.commit_nccother,
    e.commit_noncitycost,
    e.commit_total,
    e.spent_ccnonexempt,
    e.spent_ccexempt,
    e.spent_citycost,
    e.spent_nccstate,
    e.spent_nccfederal,
    e.spent_nccother,
    e.spent_noncitycost,
    e.spent_total,
    d.geom
FROM {{ ref('int__ccp_projects') }} AS a
LEFT JOIN {{ ref('int__project_checkbook_dates') }} AS c ON a.maprojid = c.maprojid
LEFT JOIN {{ ref('cpdb_dcpattributes') }} AS d ON a.maprojid = d.maprojid
LEFT JOIN {{ ref('int__budget_data_wide') }} AS e ON a.maprojid = e.maprojid
