[pytest]
# The app's source root is src/ (streamlit runs src/index.py, putting src/ on sys.path).
# Mirror that for tests so `from shared...`, `from pages...`, `from app_globals import ...`
# resolve without installing the app or relying on how pytest is launched.
pythonpath = src
