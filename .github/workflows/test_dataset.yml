name: 🧪 Test dataset
run-name: 🧪 Test dataset (${{ inputs.dataset_name }})

on:
  workflow_call:
    inputs:
      dataset_name:
        type: string
        required: true

jobs:
  sanity:
    name: Running tests for ${{ inputs.dataset_name }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo Dataset name is ${{ inputs.dataset_name }}
  template:
    if: ${{ inputs.dataset_name }} == 'template'
    uses: ./.github/workflows/template_test.yml
  # cbbr:
  #   if: ${{ inputs.dataset_name }} == 'cbbr'
  #   uses: ./.github/workflows/cbbr_test.yml