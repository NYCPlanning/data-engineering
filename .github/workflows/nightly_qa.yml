name: ⚙️ Nightly QA

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * *"

jobs:
  test:
    uses: ./.github/workflows/test_helper.yml
    secrets: inherit
  
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      dataset_name: all
      build_note: nightly qa
      run_export: true
      recipe_file: recipe
      dev_image: false
