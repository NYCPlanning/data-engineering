version: 2

models:
  - name: stg__census_blocks
    columns:
      - name: geoid
        tests:
          - not_null
          - unique

  - name: stg__low_mod_by_block_group
    columns:
      - name: geoid
        tests:
          - not_null
          - unique

  - name: stg__zap_projects
    columns:
      - name: project_id
        tests:
          - not_null
          - unique
      - name: bbls
