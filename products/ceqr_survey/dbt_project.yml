name: "dbt_ceqr_survey"

profile: "dcp-de-postgres"

model-paths: ["models"]
macro-paths: ["macros"]

tests:
  +store_failures: true
  schema: "_tests"

models:
  dbt_ceqr_survey:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    product:
      +materialized: table
